{
  "scripts": {
    "install-all": "cd client && npm install && cd ../server && npm install",
    "build-client": "cd client && npm run build && cd ../server && node server.js",
    "start": "node server/server.js"
  }
}
